#include <stdio.h>
// Somma due numeri in formato BCD packed (un byte contiene due cifre decimali, esempio 0x23 = 23). 
// Il risultato deve essere ancora BCD valido — ogni nibble deve stare tra 0 e 9. 
// Gestisci il carry tra nibble basso e nibble alto senza usare divisioni. Esempio: ft_bcd_add(0x19, 0x01) → 0x20.

unsigned char bcd_add(unsigned char a, unsigned char b) {
    
}
#include <stdio.h>

unsigned int bit_reverse_network(unsigned int n, int stages) — applica stages passi del bit-reversal usato nella FFT di Cooley-Tukey. Ad ogni stage scambia i bit secondo uno schema specifico. Trovaci la soluzione — è la generalizzazione di reverse_bits32.
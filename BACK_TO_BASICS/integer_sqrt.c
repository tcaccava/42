#include <stdio.h>
// Calcola la radice quadrata intera di n usando solo shift e confronti, senza sqrt(). 
// Esempio: n=17 → 4, n=16 → 4, n=15 → 3. L'algoritmo parte dal bit più alto possibile e lo accende o spegne in base al confronto col quadrato corrente.
unsigned int integer_sqrt(unsigned int n) {
    unsigned int min_mult = n / ;
    while(mult <= n){

    }
}